package com.cropDetails.Testing;

import static org.junit.jupiter.api.Assertions.assertThrows;

import org.junit.jupiter.api.Test;

import com.cropDetails.Exceptions.NoCropFoundException;

public class NoCropFoundExceptionTest {

    @Test
    public void testNoCropFoundException() {
        // Simulate a situation where the exception should be thrown
        assertThrows(NoCropFoundException.class, () -> {
            throw new NoCropFoundException();
        });
    }

   
}
